# Use PostgreSQL/PostGIS in deployment, for example:
# DATABASE_URL=postgresql+psycopg://floodsim:change-me@db:5432/floodsim
# The default is a local SQLite database to make Phase 2 immediately runnable.
DATABASE_URL=sqlite:///./floodsim.db
